const DAY=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
function segments(raw=[]){return raw.map(s=>({startTime:s.start_time,endTime:s.end_time,category:s.category||null,title:s.title||'',isRecurring:Boolean(s.is_recurring)})).filter(s=>s.startTime&&s.endTime)}
function overlap(a,b){return Math.max(0,(Math.min(+new Date(a.endTime),+new Date(b.endTime))-Math.max(+new Date(a.startTime),+new Date(b.startTime)))/60000)}
export function scheduleProfile(published=[],observed=[]){return published.length?{type:'published',segments:segments(published),confidence:95,evidence:`${published.length} published segment${published.length===1?'':'s'}`}:{type:observed,segments:observed.map(o=>({startTime:o.startTime,endTime:o.endTime,category:null,title:'Observed'})),confidence:observed.length?Math.min(85,45+observed.length*5):0,evidence:observed.length?`${observed.length} observed timing samples`:'No schedule evidence'}}
export function scheduleCompatibility(mine,theirs){const A=mine?.segments||[],B=theirs?.segments||[];let mins=0;for(const a of A)for(const b of B)mins+=overlap(a,b);const sharedDays=[...new Set(A.map(x=>new Date(x.startTime).getDay()))].filter(d=>B.some(x=>new Date(x.startTime).getDay()===d));const score=Math.min(100,Math.round((Math.min(mins,240)/240)*70+Math.min(sharedDays.length,3)/3*30));return {score,overlapMinutes:Math.round(mins),sharedDays:sharedDays.map(d=>DAY[d])}}
